import { createSlice } from '@reduxjs/toolkit';

const initialState={

}

export default createSlice({
        name: 'globalreducer',
        initialState,
        reducers: {
          
        },
      },
)